<MudCard Class="pa-4">
    <MudCardHeader>
        <CardHeaderContent>
            <MudText Typo="Typo.h6">标签</MudText>
        </CardHeaderContent>
    </MudCardHeader>
    <MudCardContent>
        @if (Labels.Count == 0)
        {
            <div class="empty-state">[ 暂无标签 ]</div>
        }
        else
        {
            <MudTable Items="@Labels.Select(kvp => new { Key = kvp.Key, Value = kvp.Value }).ToList()"
                      Dense="true" Hover="true" Elevation="0">
                <HeaderContent>
                    <MudTh>键</MudTh>
                    <MudTh>值</MudTh>
                </HeaderContent>
                <RowTemplate>
                    <MudTd DataLabel="键">@context.Key</MudTd>
                    <MudTd DataLabel="值">
                        @if (string.IsNullOrEmpty(context.Value))
                        {
                            <span style="max-width:200px; overflow:hidden; text-overflow:ellipsis; white-space:nowrap; display:inline-block;"></span>
                        }
                        else
                        {
                            <TextTooltip Text="@context.Value">
                                <span style="max-width:200px; overflow:hidden; text-overflow:ellipsis; white-space:nowrap; display:inline-block;">
                                    @context.Value
                                </span>
                            </TextTooltip>
                        }
                    </MudTd>
                </RowTemplate>
            </MudTable>
        }
    </MudCardContent>
</MudCard>

@code {
    [Parameter]
    public Dictionary<string, string> Labels { get; set; } = new();
}
