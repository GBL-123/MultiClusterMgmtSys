apiVersion: v1
kind: Service
metadata:
  name: 
  namespace: 
spec:
  type: LoadBalancer
  selector:
    app: 
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
      # nodePort: 30080  可省略,省略后由集群自动分配
