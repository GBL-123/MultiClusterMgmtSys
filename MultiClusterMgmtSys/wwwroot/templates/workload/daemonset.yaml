apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: 
  namespace: 
spec:
  selector:
    matchLabels:
      app: my-agent
  template:
    metadata:
      labels:
        app: my-agent
    spec:
      containers:
        - name: my-agent
          image: nginx:latest
