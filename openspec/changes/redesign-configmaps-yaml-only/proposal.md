## Why

The ConfigMap management pages (`/configmaps`, `/configmaps/{ClusterId}`, `/configmaps/{ClusterId}/{Namespace}/{Name}`, `/configmaps/{ClusterId}/{Namespace}/{Name}/{Name}/edit`, `/configmaps/{ClusterId}/{Namespace}/{Name}/yaml`) were written in the project's first-generation layout style (left TreeView cluster-picker + right self-contained content column, plus a parallel form-editor / YAML-editor pair) and have since been **commented out in place** (`@* ... *@` wrapping all five `.razor` files: `ConfigMaps.razor` 537 lines, `ConfigMapDetail.razor` 118 lines, `EditConfigMap.razor` 258 lines, `EditConfigMapYaml.razor` 152 lines, `CreateConfigMapDialog.razor` 168 lines of dead code). `Components/Layout/Drawer.razor:12` still links to `/configmaps`, so the dead pages are a live 404 path. The cluster/node features have since moved to a newer visual vocabulary (`MudPaper` toolbar + server/client-paged `MudTable` on list pages, `MudStack` of focused `MudCard`s on detail pages, established by `redesign-cluster-detail-with-endpoints`, `refactor-clusters-group-sidebar-layout`, and `redesign-nodes-page`). 

Beyond visual drift, the old design also carried a **semantic inconsistency**: a parallel form-editor path and YAML-editor path that were not equivalent (the form path silently dropped `binaryData`, never showed `labels`/`annotations`, and reused an inline hand-rolled `MudOverlay` dialog for "add key"). The redesign collapses the feature to a single **"YAML-first" mental model** — read = only-read YAML view, edit = YAML submit, create = YAML submit — bringing the page back online in the current visual vocabulary while eliminating the form/YAML parity bug.

## What Changes

- **Rewrite `Components/Configmaps/Pages/ConfigMaps.razor`** (`/configmaps`, `/configmaps/{ClusterId}`): drop the self-contained cluster picker; new layout = `<MudStack d-flex flex-auto>` with `ConfigMapListToolbar` (MudPaper: 返回集群详情 + 集群名 h4 + status chip + 刷新) → `ConfigMapListFilterBar` (MudPaper: 命名空间 `MudSelect` + 名称 `MudTextField` + 新建按钮 `AuthorizeView Roles="Admin"` gating) → `ConfigMapListTable` (`MudTable<ConfigMapListViewModel>` Items + client Pager + NoRecords). When no `ClusterId` is provided, render the "请先选择一个集群" fallback mirroring `Nodes.razor` (icon + text + 前往集群列表 button → `/clusters`) **and read `ClusterSelectionState.SelectedClusterId` to auto-redirect to `/configmaps/{id}` if a cluster was previously selected in the same circuit**. Wired to existing `ClusterService.GetClustersAsync` / `GetClusterDetailAsync` and `ConfigMapService.ListConfigMapsAsync` / `GetNamespacesAsync`.
- **Cluster context persistence refactor**: rename the existing scoped service `NodeSelectionState` (`Components/Common/NodeSelectionState.cs`) to `ClusterSelectionState` (file + class + DI registration + all call sites in `Nodes.razor` / `NodeDetail.razor`), and add new call sites in `ConfigMaps.razor`'s `LoadAsync` (after `GetClusterDetailAsync` returns non-null), `ConfigMapDetail.razor`'s `OnInitializedAsync` (on entry), `EditConfigMapYaml.razor`'s `OnInitializedAsync` (on entry), and `ClusterDetail.razor`'s `LoadAsync` (after `GetClusterDetailAsync` returns non-null). This closes the gap where switching via the Drawer between feature pages (`/nodes` ↔ `/configmaps` ↔ `/clusters/{Id}`) re-shows the "请先选择一个集群" fallback even though the user just had a cluster selected; both Nodes and Configmaps now recover to the last-visited cluster.
- **Rewrite `Components/Configmaps/Pages/ConfigMapDetail.razor`** (`/configmaps/{ClusterId}/{Namespace}/{Name}`): drop the `MudTabs` per-key only-read textareas and the in-heading back-button; new layout = `<MudStack flex-auto>` of `ConfigMapDetailToolbar` (MudPaper pa-4 mb-4: 返回列表 + `{cm.Name}` h4 + chip showing Data 键数 + 编辑 YAML button gated by `AuthorizeView Roles="Admin"` + 刷新) → `ConfigMapYamlViewCard` (MudCard Elevation=1 mb-4: read-only `MudTextField` Lines=30 monospace bound to `detail.Yaml`). Wired to existing `ConfigMapService.GetConfigMapAsync`.
- **Rewrite `Components/Configmaps/Pages/EditConfigMapYaml.razor`** (`/configmaps/{ClusterId}/{Namespace}/{Name}/yaml`): rewrite as `EditConfigMapYamlToolbar` (MudPaper pa-4 mb-4: 返回列表 + 编辑 YAML: `{Name}` h4 + 保存 button) → `ConfigMapYamlEditCard` (MudCard Elevation=1 mb-4: editable `MudTextField` Lines=30 monospace `@bind-Value=yamlContent`). Save handler runs **client-side pre-parse via `KubernetesYaml.Deserialize<V1ConfigMap>`** before calling the service; on parse failure, shows the exception message (no network call). Service layer `UpdateConfigMapFromYamlAsync` is reused unmodified (its current "only overwrites `Data` + `BinaryData`, preserves `existing` metadata" semantics is explicitly preserved as the contract).
- **Delete `Components/Configmaps/Pages/EditConfigMap.razor`** and its `/configmaps/{ClusterId}/{Namespace}/{Name}/edit` route entirely. **BREAKING** in the URL-shape sense: any bookmark to the form-editor route 404s after this change. There is no in-app link to that route after the list page's row actions are rewritten, so no internal navigation breaks.
- **Rewrite `Components/Configmaps/Shared/CreateConfigMapDialog.razor`**: collapse the form-based dialog into a single-YAML-input dialog. Dialog body = a `MudTextField` Lines=25 monospace `@bind-Value=yamlContent` pre-filled with a minimal `V1ConfigMap` template:
  ```yaml
  apiVersion: v1
  kind: ConfigMap
  metadata:
    name: 
    namespace: 
  data: {}
  ```
  Submit handler pre-parses with `KubernetesYaml.Deserialize<V1ConfigMap>`; on success calls the new `ConfigMapService.CreateConfigMapFromYamlAsync`, on failure shows the parse error. The existing `AddDataEntry` / per-key editor / namespace `MudSelect` UI is removed; users type YAML and rely on K8s API server + client pre-parse for validation.
- **Add `Components/Configmaps/Shared/ConfigMapListToolbar.razor`, `ConfigMapListFilterBar.razor`, `ConfigMapListTable.razor`, `ConfigMapDetailToolbar.razor`, `ConfigMapYamlViewCard.razor`, `ConfigMapYamlEditCard.razor`** — same component decomposition pattern as `Components/Nodes/Shared/` (mirroring `NodeListToolbar` / `NodeListFilterBar` / `NodeListTable` / `NodeDetailToolbar`).
- **Add `Components/Clusters/Shared/ClusterConfigMapsCard.razor`** — mirrors `ClusterNodesCard.razor`'s shape but with no K8s list preview (pure navigation affordance: card header with 查看全部 button → `/configmaps/{Cluster.Id}`, disabled when `!Cluster.IsReachable`). Sits on `ClusterDetail.razor` below `ClusterNodesCard` to give users an entry path into ConfigMap management from the cluster they are currently viewing.
- **Service layer**: add one new method `ConfigMapService.CreateConfigMapFromYamlAsync(int clusterId, string yaml)` that `KubernetesYaml.Deserialize<V1ConfigMap>` and calls `CreateNamespacedConfigMapAsync(body, body.Metadata.NamespaceProperty)`. The existing `CreateConfigMapAsync(ConfigMapCreateViewModel)` and `UpdateConfigMapAsync(ConfigMapUpdateViewModel)` methods are **deleted** alongside their VM types (their signatures reference those VMs, so they must move together). `GetNamespacesAsync`, `ListConfigMapsAsync`, `GetConfigMapAsync`, `DeleteConfigMapAsync`, `UpdateConfigMapFromYamlAsync` are kept unmodified.
- **VM cleanup**: delete `Components/Configmaps/ViewModels/ConfigMapCreateViewModel.cs`, `ConfigMapUpdateViewModel.cs`, `ConfigMapDataEntryViewModel.cs`. Trim `ConfigMapMappingExtensions.cs` to only the two mappers still in use (`ToConfigMapListViewModel` on `V1ConfigMap`, `ToConfigMapDetailViewModel` on `V1ConfigMap`); the `ToConfigMapDataEntryViewModel` mapper and any `ToConfigMapCreateViewModel` / `ToConfigMapUpdateViewModel` helpers (if present) are deleted. `ConfigMapListViewModel` is reused unmodified; `ConfigMapDetailViewModel.Data` is narrowed from `List<ConfigMapDataEntryViewModel>` to `Dictionary<string,string>` (preserves the `Data.Count` accessor for the toolbar chip).
- **Preserve routes** `/configmaps`, `/configmaps/{ClusterId}`, `/configmaps/{ClusterId}/{Namespace}/{Name}`, `/configmaps/{ClusterId}/{Namespace}/{Name}/yaml` verbatim; `Drawer.razor`'s existing `/configmaps` nav link becomes live again with zero edits.

## Capabilities

### New Capabilities
- `configmaps-page`: ConfigMap list (`/configmaps`, `/configmaps/{ClusterId}`), read-only YAML detail (`/configmaps/{ClusterId}/{Namespace}/{Name}`), YAML-only edit (`/configmaps/{ClusterId}/{Namespace}/{Name}/yaml`), and YAML-only create dialog, rendered in the project's current Cluster/Node visual vocabulary (toolbar + filter bar + paged table; toolbar + stacked MudCard). All write paths use YAML as the single mental model — the form-editor route and its VMs are removed.

### Modified Capabilities
- (none — existing `cluster-query-layering` is unaffected; ConfigMap pages reuse existing `ClusterService.GetClusterDetailAsync` for the context chip and `IsReachable` gate, and do not touch the cluster query spec)

## Impact

- **Code**:
  - `Components/Configmaps/Pages/ConfigMaps.razor` — rewrite (currently commented, 537 lines). Adds `ClusterSelectionState` inject + `Set` + fallback-redirect logic.
  - `Components/Configmaps/Pages/ConfigMapDetail.razor` — rewrite (currently commented, 118 lines). Adds `ClusterSelectionState.Set(ClusterId)` in `OnInitializedAsync`.
  - `Components/Configmaps/Pages/EditConfigMapYaml.razor` — rewrite (currently commented, 152 lines). Adds `ClusterSelectionState.Set(ClusterId)` in `OnInitializedAsync`.
  - `Components/Configmaps/Pages/EditConfigMap.razor` — **deleted** (currently commented, 258 lines).
  - `Components/Configmaps/Shared/CreateConfigMapDialog.razor` — rewrite (currently commented, 168 lines), dialog body collapsed to YAML-only with pre-filled minimal template.
  - `Components/Configmaps/Shared/ConfigMapListToolbar.razor`, `ConfigMapListFilterBar.razor`, `ConfigMapListTable.razor`, `ConfigMapDetailToolbar.razor`, `ConfigMapYamlViewCard.razor`, `ConfigMapYamlEditCard.razor` — new files.
  - `Components/Clusters/Shared/ClusterConfigMapsCard.razor` — new file (pure navigation entry affordance, no K8s call).
  - `Components/Clusters/Pages/ClusterDetail.razor` — adds `<ClusterConfigMapsCard Cluster="@cluster" />` below `<ClusterNodesCard>`, plus `ClusterSelectionState.Set(Id)` in `LoadAsync`.
  - `Components/Common/NodeSelectionState.cs` — **renamed** to `ClusterSelectionState.cs`, class renamed to `ClusterSelectionState`. Implementation byte-for-byte identical.
  - `Program.cs` — `AddScoped<NodeSelectionState>()` becomes `AddScoped<ClusterSelectionState>()`.
  - `Components/Nodes/Pages/Nodes.razor` — `@using ...Common` added; inject renamed to `ClusterSelectionState ClusterSelection`; all `NodeSelection.X(...)` call sites renamed to `ClusterSelection.X(...)`. Behaviour identical.
  - `Components/Nodes/Pages/NodeDetail.razor` — same rename; behaviour identical.
  - `Components/Configmaps/Services/ConfigMapService.cs` — additive: one new method `CreateConfigMapFromYamlAsync`. The two form-based methods `CreateConfigMapAsync(ConfigMapCreateViewModel)` and `UpdateConfigMapAsync(ConfigMapUpdateViewModel)` are deleted (their signatures reference the deleted VM types). `GetNamespacesAsync`, `ListConfigMapsAsync`, `GetConfigMapAsync`, `DeleteConfigMapAsync`, `UpdateConfigMapFromYamlAsync` are reused unmodified.
  - `Components/Configmaps/ViewModels/ConfigMapCreateViewModel.cs`, `ConfigMapUpdateViewModel.cs`, `ConfigMapDataEntryViewModel.cs` — **deleted**.
  - `Components/Configmaps/ViewModels/ConfigMapDetailViewModel.cs` — `Data` field narrowed from `List<ConfigMapDataEntryViewModel>` to `Dictionary<string,string>` (preserves `Data.Count` accessor for the toolbar chip).
  - `Components/Configmaps/ViewModels/Mappings/ConfigMapMappingExtensions.cs` — trimmed to only the two mappers still in use (`ToConfigMapListViewModel`, `ToConfigMapDetailViewModel`); `ToConfigMapDetailViewModel`'s `Data` assignment updated to `cm.Data?.ToDictionary(kvp => kvp.Key, kvp => kvp.Value ?? "") ?? new()`. `ConfigMapListViewModel` and `ConfigMapDetailViewModel` are reused unmodified.
  - `Components/Layout/Drawer.razor` — no code change (its existing `/configmaps` nav link target becomes live again).
- **Namespaces**: new files follow the existing `Components/Configmaps/**` rule — `MultiClusterMgmtSys.Features.Configmaps.*` (per AGENTS.md, NOT `Components.*`). `_Imports.razor` is unchanged; each new page/component carries its own `@using MultiClusterMgmtSys.Features.Configmaps.Services` / `...ViewModels` / `...ViewModels.Mappings` lines as needed, mirroring the commented originals in `ConfigMaps.razor` and the live precedent in `Nodes.razor`.
- **Services/APIs**: `ConfigMapService` gains `CreateConfigMapFromYamlAsync`; no repository change; no DB impact; no new NuGet packages. `ClusterService` reused unmodified.
- **Database**: none. ConfigMaps are a live K8s read/write; no entity change, no migration, no `EnsureCreated` impact.
- **Dependencies**: no new NuGet packages; uses only MudBlazor 9 + existing `Microsoft.AspNetCore.Authorization` (`AuthorizeView`) + `KubernetesYaml` (already referenced via `KubernetesClient` 19).
- **Routes**: `/configmaps`, `/configmaps/{ClusterId}`, `/configmaps/{ClusterId}/{Namespace}/{Name}`, `/configmaps/{ClusterId}/{Namespace}/{Name}/yaml` are restored to working routes (today they resolve to commented Razor and 404). `/configmaps/{ClusterId}/{Namespace}/{Name}/edit` is **removed**; any bookmark 404s.
- **Tests**: no test project exists in the repo; manual smoke verified via `dotnet build` + `dotnet run` against a real K8s cluster (list / detail / YAML edit / create / delete round-trip, plus a binaryData-bearing ConfigMap to verify the YAML round-trip is symmetric with what the API server persists).