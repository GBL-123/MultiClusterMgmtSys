## 1. Prerequisite: re-read sibling files for namespace/style anchoring

- [x] 1.1 Open `Components/Nodes/Shared/NodeListToolbar.razor` and `NodeListFilterBar.razor` and confirm the `MudPaper pa-4 mb-4` + `[Parameter]` + `EventCallback` convention to mirror in ConfigMap shared components
- [x] 1.2 Open `Components/Nodes/Shared/NodeListTable.razor` and `Components/Nodes/Pages/Nodes.razor` to confirm the `MudTable Items + Pager + NoRecordsContent` shape to mirror
- [x] 1.3 Open `Components/Nodes/Shared/NodeDetailToolbar.razor` and `Components/Clusters/Shared/ClusterOverviewCard.razor` to confirm the detail-page Toolbar + single MudCard shape to mirror
- [x] 1.4 Open the commented `Components/Configmaps/Pages/ConfigMaps.razor` (lines 1-30) to confirm the existing `@using MultiClusterMgmtSys.Features.Configmaps.*` namespace lines that must be preserved (NOT `Components.*`)
- [x] 1.5 Open `Components/Configmaps/ViewModels/Mappings/ConfigMapMappingExtensions.cs` to confirm which existing mappers `ConfigMapService` consumes, so the trim in §7 removes only the dead ones
- [x] 1.6 Open `Components/Clusters/Pages/Clusters.razor`, `Components/Clusters/Shared/ClusterTable.razor`, and `Components/Clusters/Shared/ClusterFilterBar.razor` to confirm the list shell to mirror for the ConfigMap list (single `MudPaper` with inlined title row + merged filter bar; table with `Hover` + `FixedHeader` + `MudTableSortLabel` sortable headers + `LoadingContent`)

## 2. Service layer: add CreateConfigMapFromYamlAsync

- [x] 2.1 Add `using k8s.Models;` if not present (it already is, per current line 2 of `ConfigMapService.cs`) and verify `KubernetesYaml` is reachable
- [x] 2.2 Implement `public async Task CreateConfigMapFromYamlAsync(int clusterId, string yaml)` in `Components/Configmaps/Services/ConfigMapService.cs` following design.md Decision 7: `repo.GetByIdAsync` → `BuildConfig` → `KubernetesYaml.Deserialize<V1ConfigMap>(yaml)` → `client.CoreV1.CreateNamespacedConfigMapAsync(body, body.Metadata?.NamespaceProperty)` with the null-namespace `InvalidOperationException`
- [x] 2.3 Verify `CreateConfigMapAsync(ConfigMapCreateViewModel)` and `UpdateConfigMapAsync(ConfigMapUpdateViewModel)` methods are **deleted** from `ConfigMapService.cs` (Decision 7 revised during apply: keeping the dead methods would force keeping the dead VMs they reference, contradicting Decision 10; the two are deleted together).

## 3. Shared components: renderers (cards first, bottom-up)

- [x] 3.1 Create `Components/Configmaps/Shared/ConfigMapYamlViewCard.razor` — `MudCard Elevation=1 Class="mb-4"` + read-only `MudTextField` Lines=30 monospace bound to `[Parameter] string Yaml`. Empty-state "暂无 YAML" when null/empty. Namespace `MultiClusterMgmtSys.Features.Configmaps.Shared`.
- [x] 3.2 Create `Components/Configmaps/Shared/ConfigMapYamlEditCard.razor` — `MudCard Elevation=1 Class="mb-4"` + editable `MudTextField` Lines=30 monospace, two-way bindable `Yaml`/`YamlChanged` `EventCallback<string>`. Save handler lives in the parent page, not the card. Same namespace as §3.1.
- [x] 3.3 REWORK (iteration 4): restyle `Components/Configmaps/Shared/ConfigMapListTable.razor` — `MudTable<ConfigMapListViewModel>` Items + `Hover` + `FixedHeader` (remove `Dense` and `Elevation=0`), client `Pager` (RowsPerPageString="每页条数：" InfoFormat="共 {all_items} 条"), `LoadingContent` (正在加载...), `NoRecordsContent` (暂无 ConfigMap 或没有符合筛选条件的 ConfigMap). Sortable headers via `MudTableSortLabel` for 名称 / 命名空间 / Data 键数 / 创建时间 (client-side sorting, Items mode). Six columns: 名称 (clickable), 命名空间 `MudChip Small`, Data 键数, 键名预览 (ellipsis), 创建时间 (`yyyy-MM-dd HH:mm`), 操作 icons. Params: `Items`, `Loading` (bool), `OnNavigateDetail(string ns, string name)`, `OnNavigateEditYaml(string ns, string name)`, `OnDelete(string ns, string name)` EventCallbacks. 编辑 YAML and 删除 icons wrapped in `<AuthorizeView Roles="Admin">`. Visual attributes mirror `ClusterTable.razor`.
- [x] 3.4 REWORK (iteration 4): restyle `Components/Configmaps/Shared/ConfigMapListFilterBar.razor` — remove its `MudPaper` wrapper (sits inside the page's single `MudPaper` below the title row, mirroring `ClusterFilterBar`'s placement): 命名空间 `MudSelect<string?>` (leading 全部命名空间 `<MudSelectItem Value="@((string?)null)">` option) + 名称 `MudTextField<string>` + trailing 查询 `MudButton` (Variant.Filled, Color.Primary, StartIcon=Search) + 重置 `MudButton` (Variant.Outlined, Color.Secondary, StartIcon=Refresh). REMOVE the 新建 ConfigMap button (moved to the title row) and the `Cluster` param. Params: `Namespaces` (`List<string>`), `SelectedNamespace` (two-way `string?`), `SearchName` (two-way `string`), `OnQuery`, `OnReset` EventCallbacks.
- [x] 3.5 REWORK (iteration 4): DELETE the file `Components/Configmaps/Shared/ConfigMapListToolbar.razor` and inline its content into `ConfigMaps.razor`'s single `MudPaper` (mirrors `Clusters.razor`): 配置管理 h5 + 集群名/状态 `MudChip` (via the `GetClusterStatusColor` helper) + 刷新 `MudButton` (Variant.Text, Color.Inherit, Disabled=Processing) + 新建 ConfigMap `MudButton` (Variant.Filled, Color.Primary, StartIcon=Add) inside `<AuthorizeView Roles="Admin">` Disabled=`@(cluster is null || !cluster.IsReachable)`. The 返回集群详情 back button from the iteration-3 toolbar is NOT carried over (removed per user decision — sidebar highlight covers the navigation).
- [x] 3.6 Create `Components/Configmaps/Shared/ConfigMapDetailToolbar.razor` — `MudPaper pa-4 mb-4`: 返回列表 `MudButton` (navigates to `/configmaps/{ClusterId}`) + `{Name}` h4 + `MudChip` showing `Data 键数: {Detail.Data.Count}` (compute inline) + 编辑 YAML `MudButton` wrapped in `<AuthorizeView Roles="Admin">` + 刷新 `MudButton`. Params: `ClusterId` (int), `Name` (string), `Detail` (`ConfigMapDetailViewModel`), `Processing`, `OnRefresh`, `OnEditYaml` EventCallbacks.
- [x] 3.7 Rewrite `Components/Configmaps/Shared/CreateConfigMapDialog.razor` (overwrite the commented file) — `MudDialog` body = single `MudTextField` Lines=25 monospace `@bind-Value=yamlContent`; `OnInitializedAsync` populates `yamlContent` with the minimal `V1ConfigMap` template from design.md Decision 6. `DialogActions`: 取消 (OnClick Dialog.Cancel) + 创建 (OnClick Submit). Submit handler: `try { KubernetesYaml.Deserialize<V1ConfigMap>(yamlContent); } catch (Exception ex) { Snackbar.Add($"YAML 格式错误: {ex.Message}", Severity.Error); return; }` then `await ConfigMapService.CreateConfigMapFromYamlAsync(ClusterId, yamlContent)` with the existing 409/Conflict → 同名 ConfigMap 已存在 mapping (copied from commented `CreateConfigMapDialog.razor:157-162`). On success: `Snackbar.Add("创建成功")` + `Dialog.Close(DialogResult.Ok(true))`. Params: `ClusterId` (int), `[CascadingParameter] IMudDialogInstance Dialog`.
- [x] 3.8 Create `Components/Configmaps/Shared/ConfigMapClusterSidebar.razor` — byte-for-byte mirror of `Components/Nodes/Shared/NodeClusterSidebar.razor` except the CSS class prefix (`configmap-*`) and the explicit `@namespace MultiClusterMgmtSys.Features.Configmaps.Shared`. 240px `MudPaper` (集群选择 header + `MudNavMenu`), groups clusters by `GroupName` ("未分组" last), per-group collapsible, status color-dot per cluster row (`ClusterStatus` → hex green/red/gray), active-link highlight for `SelectedClusterId`, NO search box. Params: `Clusters` (`IReadOnlyList<ClusterViewModel>`), `SelectedClusterId` (`int?`), `OnClusterSelected` (`EventCallback<int>`). Add `@using MultiClusterMgmtSys.Common.Enums` + `@using MultiClusterMgmtSys.Components.Clusters.ViewModels`.

## 4. Pages: detail page first (smallest scope)

- [x] 4.1 Rewrite `Components/Configmaps/Pages/ConfigMapDetail.razor` (overwrite the commented file) with `@attribute [Authorize]`, `@page "/configmaps/{ClusterId:int}/{Namespace}/{Name}"`. Layout = `<MudStack Class="flex-auto">` containing `<ConfigMapDetailToolbar .../>` + `<ConfigMapYamlViewCard Yaml="@detail?.Yaml"/>`. `@code` block injects `ConfigMapService`, `ISnackbar`, `NavigationManager`; `OnInitializedAsync` calls `GetConfigMapAsync(ClusterId, Name, Namespace)`, sets `detail`, handles null with the ConfigMap 不存在或已被删除 empty state card. Carry `@using MultiClusterMgmtSys.Features.Configmaps.Services`, `@using MultiClusterMgmtSys.Features.Configmaps.ViewModels`, `@using MultiClusterMgmtSys.Features.Configmaps.Shared` as needed.
- [x] 4.2 Verify the "返回列表" button in `ConfigMapDetailToolbar` correctly resolves to `/configmaps/{ClusterId}` and the chip renders "Data 键数: {detail.Data.Count}"

## 5. Pages: YAML editor page

- [x] 5.1 Rewrite `Components/Configmaps/Pages/EditConfigMapYaml.razor` (overwrite the commented file) with `@attribute [Authorize(Roles="Admin")]`, `@page "/configmaps/{ClusterId:int}/{Namespace}/{Name}/yaml"`. Layout = `<MudStack Class="flex-auto">` containing a small inline `EditConfigMapYamlToolbar` block (or inline `MudPaper pa-4 mb-4` if a separate component is not added — design.md Decision 2 lists this as part of the page, not a separate shared component; implement inline inside the page) with 返回列表 (→ `/configmaps/{ClusterId}`) + 编辑 YAML: `{Name}` h4 + 保存 `MudButton` + `<ConfigMapYamlEditCard @bind-Yaml="yamlContent"/>`.
- [x] 5.2 Implement `OnInitializedAsync` loading `GetConfigMapAsync(ClusterId, Name, Namespace)` → `yamlContent = detail.Yaml`; null → ConfigMap 不存在或已被删除 empty state card.
- [x] 5.3 Implement the Save handler with the pre-parse step from design.md Decision 11: `try { KubernetesYaml.Deserialize<V1ConfigMap>(yamlContent); } catch (Exception ex) { Snackbar.Add($"YAML 格式错误: {ex.Message}", Severity.Error); return; }` then `await ConfigMapService.UpdateConfigMapFromYamlAsync(ClusterId, Name, Namespace, yamlContent)`. On success: `Snackbar.Add("修改成功")` + `NavigateTo($"/configmaps/{ClusterId}")`. On 409/Conflict: `Snackbar.Add("资源已被他人修改，请刷新后重试", Severity.Warning)`. Other exceptions: `Snackbar.Add($"修改失败: {ex.Message}", Severity.Error)`.
- [x] 5.4 Verify the Save button shows `MudProgressCircular Small Indeterminate` while `saving` is true, Disabled otherwise.

## 6. Pages: list page

- [x] 6.1 Rewrite `Components/Configmaps/Pages/ConfigMaps.razor` (overwrite the commented file) with `@attribute [Authorize]`, `@page "/configmaps"`, `@page "/configmaps/{ClusterId:int}"`. Layout = `<MudStack Row="true" Class="d-flex flex-auto">` containing `<ConfigMapClusterSidebar .../>` (left) + a right `<MudStack Class="flex-auto">` pane. The right pane renders the 请从左侧选择一个集群 empty state (icon + h6, NO 前往集群列表 button) when `!EffectiveClusterId.HasValue`, mirroring the current `Nodes.razor`.
- [x] 6.2 Implement the loading / cluster-not-found / unreachable states mirroring the current `Nodes.razor`: `MudProgressLinear` while loading; 未找到该集群 card with 返回集群列表 button (→ `/clusters`) when `cluster is null`; 集群不可达 card (集群不可达，无法获取 ConfigMap) when `!cluster.IsReachable` (the toolbar renders in both the reachable and unreachable branches, as in `Nodes.razor`).
- [x] 6.3 REWORK (iteration 4): in the cluster-OK branch, render the single `MudPaper pa-4` (mirroring `Clusters.razor`): title row `[配置管理 h5] [集群名/状态 MudChip] [Spacer] [刷新] [新建 ConfigMap Admin-gated, Disabled when !cluster.IsReachable]` — NO 返回集群详情 button (removed per user decision) — + merged `<ConfigMapListFilterBar Namespaces="@namespaces" SelectedNamespace="@selectedNamespace" SearchName="@searchName" OnQuery="OnQueryClick" OnReset="OnResetClick"/>` (filter bar only when reachable), then `<ConfigMapListTable Items="@filteredConfigMaps" Loading="@loading" OnNavigateDetail="NavigateToDetail" OnNavigateEditYaml="NavigateToYamlEdit" OnDelete="DeleteConfigMap"/>` when reachable. The sidebar renders outside all branches: `<ConfigMapClusterSidebar Clusters="@clusters" SelectedClusterId="@EffectiveClusterId" OnClusterSelected="SelectCluster"/>` where `SelectCluster` `NavigateTo`s `/configmaps/{id}`.
- [x] 6.4 Implement `@code` block: `[Parameter] int? ClusterId`, `previousClusterId` + `hasInitialized` first-frame guard in `OnParametersSetAsync` (`if (ClusterId == previousClusterId && hasInitialized) return;` — both sides default to `null` for `int?`, so a naive diff skips the first frame; mirror the current `Nodes.razor`), and `EffectiveClusterId => ClusterId ?? ClusterSelection.SelectedClusterId`. `clusters` (sidebar data) loads once in `OnInitializedAsync` via `ClusterService.GetPagedAsync` (PageSize=1000, name sort). The parameterless route restores the remembered selection in place — no `NavigateTo`, no landing card.
- [x] 6.5 Remove the cluster-picker TreeView code from the old `ConfigMaps.razor` — it is replaced by the left `ConfigMapClusterSidebar` fed from `GetPagedAsync`. Sidebar click → `NavigateTo($"/configmaps/{id}")`; there is no `/clusters` fallback button.
- [x] 6.6 Implement `LoadAsync` (refresh): `cluster = await ClusterService.GetClusterDetailAsync(ClusterId.Value)`; if reachable, `namespaces = await ConfigMapService.GetNamespacesAsync(ClusterId.Value)` and `configMaps = await ConfigMapService.ListConfigMapsAsync(ClusterId.Value, selectedNamespace)`; wrap each in try/catch with appropriate Snackbar severity (Warning for namespaces, Error for ConfigMaps, Error for cluster detail) — copy verbatim from commented `ConfigMaps.razor:394-434`.
- [x] 6.7 REWORK (iteration 4): replace the live-filter handlers with `OnQueryClick()`: re-call `ConfigMapService.ListConfigMapsAsync(EffectiveClusterId.Value, selectedNamespace)` with loading state (namespace filter applies only on 查询, per Decision 13); and `OnResetClick()`: `selectedNamespace = null` + `searchName = ""` then re-call `LoadAsync` to refresh namespaces + list.
- [x] 6.8 REWORK (iteration 4): keep `filteredConfigMaps` as a client-side filter on `cm.Name.Contains(searchName, StringComparison.OrdinalIgnoreCase)` — it is evaluated only after 查询 is clicked (the field binds two-way; the filter property applies on render). Copy the predicate from commented `ConfigMaps.razor:339-348`.
- [x] 6.9 Implement `OpenCreateDialog`: `DialogService.ShowAsync<CreateConfigMapDialog>("新建 ConfigMap", new DialogParameters { { "ClusterId", ClusterId!.Value } }, new DialogOptions { CloseOnEscapeKey = true, MaxWidth = MaxWidth.Medium, FullWidth = true })`; on `!result.Canceled` call `LoadAsync`. Copy pattern from commented `ConfigMaps.razor:512-520`.
- [x] 6.10 Implement `DeleteConfigMap(string ns, string name)`: `DialogService.ShowMessageBoxAsync("确认删除", $"确认删除 ConfigMap「{name}」？此操作不可撤销。", yesText: "删除", noText: "取消")`; on confirm call `ConfigMapService.DeleteConfigMapAsync(ClusterId!.Value, name, ns)`, 404-aware error mapping (copy from commented `ConfigMaps.razor:458-483`), then `LoadAsync`.
- [x] 6.11 Implement `NavigateToDetail(ns, name)`, `NavigateToYamlEdit(ns, name)` callbacks that call `NavigationManager.NavigateTo($"/configmaps/{ClusterId!.Value}/{ns}/{name}")` and `.../yaml` respectively. The NavigateToEdit (form-editor) callback from the old code is not re-implemented — there's no form-editor route anymore.
- [x] 6.12 Carry `@using MultiClusterMgmtSys.Features.Configmaps.Services`, `@using MultiClusterMgmtSys.Features.Configmaps.ViewModels`, `@using MultiClusterMgmtSys.Features.Configmaps.Shared`, `@using MultiClusterMgmtSys.Components.Clusters.Services` (for `ClusterService`), `@using MultiClusterMgmtSys.Components.Clusters.ViewModels` (for `ClusterDetailViewModel` / `ClusterViewModel`), `@using MultiClusterMgmtSys.Components.Clusters.Requests` (for `ClusterQueryRequest`), `@using MultiClusterMgmtSys.Common.Enums` (for `ClusterSortField` / `ClusterStatus`), `@using MultiClusterMgmtSys.Components.Common` (for `ClusterSelectionState`).

## 7. Remove dead artifacts

- [x] 7.1 Delete `Components/Configmaps/Pages/EditConfigMap.razor` (the commented form-editor page). This removes the `/configmaps/{ClusterId:int}/{Namespace}/{Name}/edit` route entirely.
- [x] 7.2 Delete `Components/Configmaps/ViewModels/ConfigMapCreateViewModel.cs`.
- [x] 7.3 Delete `Components/Configmaps/ViewModels/ConfigMapUpdateViewModel.cs`.
- [x] 7.4 Delete `Components/Configmaps/ViewModels/ConfigMapDataEntryViewModel.cs`.
- [x] 7.5a Modify `Components/Configmaps/ViewModels/ConfigMapDetailViewModel.cs` to change `public List<ConfigMapDataEntryViewModel> Data { get; set; } = new();` to `public Dictionary<string, string> Data { get; set; } = new();`. Preserves `Data.Count` accessor for the detail-toolbar chip.
- [x] 7.5 Trim `Components/Configmaps/ViewModels/Mappings/ConfigMapMappingExtensions.cs` to keep only the mappers that survive: `ToConfigMapListViewModel(this V1ConfigMap)` and `ToConfigMapDetailViewModel(this V1ConfigMap)`. In `ToConfigMapDetailViewModel`, change the `Data` assignment from `cm.Data?.Select(kvp => new ConfigMapDataEntryViewModel { Key = kvp.Key, Value = kvp.Value ?? "" }).ToList() ?? new()` to `cm.Data?.ToDictionary(kvp => kvp.Key, kvp => kvp.Value ?? "") ?? new()`. `Yaml = KubernetesYaml.Serialize(cm)` unchanged. Delete any `ToConfigMapDataEntryViewModel` / `ToConfigMapCreateViewModel` / `ToConfigMapUpdateViewModel` helpers present (none currently exist beyond the two above).
- [x] 7.6 Verify the surviving mappers still satisfy `ConfigMapService.ListConfigMapsAsync` and `GetConfigMapAsync` callers (they are the only consumers of these mappers).

## 8. Sanity: verify no remaining callers of dead service methods

- [x] 8.1 `rg "CreateConfigMapAsync"` across the repo — should find only the new `CreateConfigMapFromYamlAsync` declaration (different name). The dead `CreateConfigMapAsync(ConfigMapCreateViewModel)` is deleted per revised Decision 7.
- [x] 8.2 `rg "UpdateConfigMapAsync"` across the repo — should find only `UpdateConfigMapFromYamlAsync`. The dead `UpdateConfigMapAsync(ConfigMapUpdateViewModel)` is deleted per revised Decision 7.
- [x] 8.3 `rg "ConfigMapCreateViewModel|ConfigMapUpdateViewModel|ConfigMapDataEntryViewModel"` across the codebase — should return zero matches in `.cs`/`.razor` files (all three types deleted, all references removed, dead service methods deleted). Matches in `openspec/` artifact docs remain as design record and are acceptable.

## 9. Build + smoke

- [x] 9.1 Run `dotnet build MultiClusterMgmtSys.slnx` — must complete with zero errors. Warnings about unused service methods (if the compiler emits any; C# typically does not for public methods on DI services) are acceptable per Decision 7.
- [ ] 9.2 Delete local `MultiClusterMgmtSys.db` (if present) to start clean, then `dotnet run --project MultiClusterMgmtSys`. Login as `admin` / `Changeme_123`.  ⏸ MANUAL (requires user to run app + browser + K8s cluster)
- [ ] 9.3 Smoke: navigate to `/configmaps` in a fresh circuit and confirm the left `ConfigMapClusterSidebar` renders (grouped clusters, collapsible groups, status color-dots) with the 请从左侧选择一个集群 empty state in the right pane.  ⏸ MANUAL
- [ ] 9.4 Smoke: click a cluster row in the sidebar, confirm navigation to `/configmaps/{id}` showing the clusters-style shell — a single `MudPaper` with the title row (配置管理 h5 + 集群名/状态 chip + 刷新 + 新建 ConfigMap, NO back button) and the merged filter bar (命名空间/名称/查询/重置), plus the sortable `ClusterTable`-style table on a reachable cluster; then click Drawer 配置管理 and confirm the right pane restores the same cluster in place (URL stays `/configmaps`).  ⏸ MANUAL
- [ ] 9.5 Smoke: confirm the 命名空间 `MudSelect` lists namespaces returned by the cluster, and that changing it does NOT re-load the table; clicking 查询 re-loads the table for the selected namespace.  ⏸ MANUAL
- [ ] 9.6 Smoke: confirm the 名称 search applies the client-side filter only after clicking 查询 (no network call on typing), and that 重置 clears namespace + name and re-fetches the full list.  ⏸ MANUAL
- [ ] 9.7 Smoke: click 详情 on a row, confirm the detail page renders the read-only YAML card with the full `V1ConfigMap` YAML (including `data`, `binaryData`, `labels`, `annotations`, `metadata`).  ⏸ MANUAL
- [ ] 9.8 Smoke: as Admin, click 编辑 YAML on a row, confirm the YAML editor page loads with the YAML pre-filled. Edit a non-trivial change (e.g., add a `data` key), click 保存, confirm 修改成功 snackbar and navigation back to `/configmaps/{ClusterId}`, and confirm the row's 键名预览 reflects the new key on refresh.  ⏸ MANUAL
- [ ] 9.9 Smoke: as Admin, open the create dialog, confirm the YAML field is pre-filled with the minimal template. Type a malformed YAML, click 创建, confirm the YAML 格式错误 snackbar appears and the dialog stays open. Then type a valid `V1ConfigMap` YAML with a real namespace, click 创建, confirm the dialog closes, the 创建成功 snackbar appears, and the new ConfigMap appears in the list.  ⏸ MANUAL
- [ ] 9.10 Smoke: as Admin, click 删除 on a row, confirm the 确认删除 dialog appears. Confirm. Confirm the 删除成功 snackbar and the row's disappearance on refresh.  ⏸ MANUAL
- [ ] 9.11 Smoke: as Admin, test a `binaryData`-bearing ConfigMap round-trip — create one via YAML with `binaryData: { key: !!binary BASE64 }`, read it back via 详情 (confirm `binaryData` appears in the YAML), edit the YAML (remove the `binaryData` block), save, and confirm the detail page no longer shows `binaryData`. This verifies the R5 by-design behavior (documented as a theoretical sharp edge; target scenario does not involve binaryData).  ⏸ MANUAL
- [ ] 9.12 Smoke: as non-Admin (login as `member` if seeded; otherwise skip and document), confirm the 新建 ConfigMap button, the row 编辑 YAML / 删除 icons, and the detail page 编辑 YAML button are all hidden. Read paths (list, detail, 刷新) all work.  ⏸ MANUAL
- [ ] 9.13 Smoke: confirm a stale bookmark to `/configmaps/1/default/some-cm/edit` returns the app's not-found behavior (404 page or fallback).  ⏸ MANUAL

## 10. ClusterSelectionState refactor (discovered during apply)

- [x] 10.1 Rename `Components/Common/NodeSelectionState.cs` → `ClusterSelectionState.cs` (class name `NodeSelectionState` → `ClusterSelectionState`). Implementation byte-for-byte identical.
- [x] 10.2 Update `Program.cs`: `AddScoped<NodeSelectionState>()` → `AddScoped<ClusterSelectionState>()`.
- [x] 10.3 Update `Components/Nodes/Pages/Nodes.razor`: add `@using MultiClusterMgmtSys.Components.Common`; inject renamed `ClusterSelectionState ClusterSelection`; all `NodeSelection.X(...)` → `ClusterSelection.X(...)` (3 call sites: `OnParametersSet`, `OnParametersSetAsync` fallback branch's read + NavigateTo).
- [x] 10.4 Update `Components/Nodes/Pages/NodeDetail.razor`: same rename; 2 call sites (`@using` + inject + `OnParametersSet`'s `Set(ClusterId)`).
- [x] 10.5 Update `Components/Configmaps/Pages/ConfigMaps.razor`: add `@using MultiClusterMgmtSys.Components.Common` + `ClusterSelectionState ClusterSelection` inject. Call `ClusterSelection.Set(ClusterId.Value)` in `OnParametersSet` when `ClusterId.HasValue`, and again in `LoadAsync` after `ClusterService.GetClusterDetailAsync` returns non-null. The parameterless route reads `ClusterSelection.SelectedClusterId` via `EffectiveClusterId` and restores the list IN PLACE — no `NavigateTo`, no static fallback.
- [x] 10.6 Update `Components/Configmaps/Pages/ConfigMapDetail.razor`: add `@using MultiClusterMgmtSys.Components.Common` + `ClusterSelectionState ClusterSelection` inject. In `OnInitializedAsync`, call `ClusterSelection.Set(ClusterId)` before `LoadAsync` (mirrors `NodeDetail.razor`).
- [x] 10.7 Update `Components/Configmaps/Pages/EditConfigMapYaml.razor`: same inject + `ClusterSelection.Set(ClusterId)` on entry in `OnInitializedAsync`.
- [x] 10.8 Update `Components/Clusters/Pages/ClusterDetail.razor`: add `@using MultiClusterMgmtSys.Components.Common` + `ClusterSelectionState ClusterSelection` inject; in `LoadAsync`, call `ClusterSelection.Set(Id)` after `GetClusterDetailAsync` returns non-null.
- [x] 10.9 Run `dotnet build MultiClusterMgmtSys.slnx` — must complete with zero errors. Verify no remaining `NodeSelectionState` references in `.cs`/`.razor` files.
- [ ] 10.10 Smoke: from `/configmaps/3`, click Drawer 配置管理, confirm the URL becomes `/configmaps` and the right pane restores cluster 3's ConfigMap list in place with the sidebar row highlighted (no empty state — because `ClusterSelectionState.SelectedClusterId` is 3).  ⏸ MANUAL
- [ ] 10.11 Smoke: from `/configmaps/3`, click Drawer 节点管理, confirm the `/nodes` page restores cluster 3's node list in place (URL stays `/nodes`, sidebar row highlighted — ConfigMaps set `ClusterSelectionState.SelectedClusterId` = 3 and `Nodes.razor` reads it).  ⏸ MANUAL
- [ ] 10.12 Smoke: from `/clusters/3` (cluster detail), click Drawer 节点管理, confirm the `/nodes` page restores cluster 3 in place (URL stays `/nodes` — `ClusterDetail.razor::LoadAsync` set `ClusterSelectionState.SelectedClusterId` = 3).  ⏸ MANUAL
- [ ] 10.13 Smoke: open `/configmaps` in a fresh browser tab (no prior selection), confirm the sidebar renders with the 请从左侧选择一个集群 empty state in the right pane (because the new circuit's `ClusterSelectionState.SelectedClusterId` is null).  ⏸ MANUAL
